import React from "react";
import className from "classnames/bind";
import styles from "./Resources.module.scss";
const cx = className.bind(styles);

function Resources() {
  return <div className={cx("wrapper")}>Resources</div>;
}

export default Resources;
